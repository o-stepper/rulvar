/**
 * @lurker/plan: lurker adaptive orchestration extension: PlanRunner, RunLedger, escalation extensions, ModelLadder configuration.
 *
 * M0 scaffold (v0.1.0): no public API yet. The first real surface of this
 * package ships in milestone M7 per docs/10-implementation-plan.md.
 */

/**
 * Temporary M0 scaffold marker (M0-T02 acceptance: a sample exported symbol
 * round-trips through build and packs). Removed when the package's first
 * real API lands.
 */
export const M0_SCAFFOLD = '@lurker/plan' as const;
