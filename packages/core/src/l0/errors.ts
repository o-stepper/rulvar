/**
 * L0 error taxonomy: LurkerError base with the closed string-code registry,
 * the WireError JSON-serializable projection, and every named error class
 * (defined up front so the code registry closes exactly once).
 *
 * Owning spec: docs/02-architecture.md, section "Error taxonomy". Populated
 * by M1-T02; this file is the M0-T08 skeleton and deliberately exports
 * nothing yet.
 */
export {};
