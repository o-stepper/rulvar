/**
 * SchemaSpec (three forms), Out<S> inference, JSON Schema projection via
 * StandardJSONSchemaV1, and canonical schema derivation feeding schemaHash.
 * Backed by the vendored StandardSchemaV1/StandardJSONSchemaV1 declarations
 * (src/vendor/standard-schema.d.ts, types only) and the vendored validator
 * (src/vendor/json-schema/).
 *
 * Owning spec: docs/08-tools-permissions-spec.md, section "SchemaSpec".
 * Populated by M1-T03; this file is the M0-T08 skeleton and deliberately
 * exports nothing yet.
 */
export {};
