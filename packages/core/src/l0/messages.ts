/**
 * L0 wire contracts: Msg and the Part union (including provider-raw),
 * ChatRequest, the ChatEvent union, Usage and its invariant, CanonicalId
 * (engine-minted ULID), cacheHint, the canonical Effort union, the L0
 * aliases JsonSchema and ToolContract, and the model-spec family
 * (ModelSpec, ModelChoice, CanonicalModelSpec, CanonicalLadderSpec).
 *
 * Owning spec: docs/04-model-layer-spec.md, sections "Wire contract (L0)"
 * and "Router and resolution chain". Populated by M1-T01; this file is the
 * M0-T08 skeleton and deliberately exports nothing yet.
 */
export {};
