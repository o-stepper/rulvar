# rulvar (pointer package)

The canonical package is [`@rulvar/rulvar`](https://www.npmjs.com/package/@rulvar/rulvar); this unscoped name is an alias that re-exports it, so `npm install rulvar` works and the name stays reserved for the project.

Project home: [rulvar.com](https://rulvar.com) and [github.com/o-stepper/rulvar](https://github.com/o-stepper/rulvar).
